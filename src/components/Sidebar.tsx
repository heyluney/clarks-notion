import "./Sidebar.css"

// need to render every page 
const SideBar = () => {
    return (
        <div className="sidebar">
            this is the sidebar
        </div>
    )
}

export default SideBar;